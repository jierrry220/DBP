# 🎵 音效系统快速测试

## 当前状态
✅ **v1.2.3 - 自动解锁版本**
- ✅ 页面刷新后音效自动恢复
- ✅ 自动解锁音频上下文：用户首次点击/滚动后，后续刷新都能自动播放
- ✅ 无需每次刷新都点击页面

## 测试步骤

### 1. 新游戏开始测试
1. 打开游戏页面
2. 等待新一局游戏开始
3. 检查浏览器控制台 (F12)

**预期日志：**
```
[音效] 游戏开始，播放BGM
[音效] ✅ BGM 播放成功!  ← 成功
```

**或者：**
```
[音效] 游戏开始，播放BGM
[音效] ⚠️ BGM播放被浏览器阻止  ← 需要用户交互
[音效] 📢 设置用户交互监听器
```

### 2. 如果看到"BGM播放被阻止"
- 点击页面任意位置（投注按钮、房间、滚动等）
- BGM 应该立即开始播放
- 会显示提示：`🎵 点击页面任意位置启动音效`

### 3. 🔓 **自动解锁测试（新增）**

#### 重要：首次访问需要交互一次
1. 首次打开页面时，**点击任意位置**或**滚动一下**
2. 控制台会显示：`[音效] 🔓 检测到用户交互，解锁音频上下文`
3. 然后显示：`[音效] ✅ 音频上下文已解锁，现在可以自动播放了`
4. **之后所有刷新都不再需要点击，音频会自动播放！**

💡 **原理**：浏览器的自动播放策略要求用户与页面交互过才能播放音频。我们在首次交互时解锁了音频上下文，之后就可以自由播放了。

---

### 4. 🔄 **页面刷新测试**

#### 4.1 在投注阶段刷新（倒计时 > 10秒）
1. 等待游戏进入投注阶段
2. 确认 BGM 正在播放
3. 按 F5 刷新页面
4. **预期结果**：BGM 应该立即恢复播放（如果被浏览器阻止，点击一下就能恢复）

**预期日志：**
```
[音效管理器] 🎵 开始初始化...
[音效管理器] ✅ 初始化完成!
[音效] 🔄 恢复游戏状态: 阶段=betting, 倒计时=45s
[音效] 恢复：投注阶段，播放BGM
[音效] ✅ BGM 恢复播放成功
```

#### 3.2 在倒计时最后10秒刷新
1. 等待倒计时进入最后10秒
2. 确认听到 "哔~哔~哔~" 声
3. 按 F5 刷新页面
4. **预期结果**：刷新后立即恢复倒计时音效

**预期日志：**
```
[音效] 🔄 恢复游戏状态: 阶段=betting, 倒计时=7s
[音效] 恢复：倒计时最后10秒，播放哔哔声
(立即听到哔声)
```

#### 3.3 在杀手/结算阶段刷新
1. 等待杀手阶段或结算阶段
2. 按 F5 刷新页面
3. **预期结果**：保持静音

**预期日志：**
```
[音效] 🔄 恢复游戏状态: 阶段=killer_moving, 倒计时=5s
[音效] 恢复：杀手/结算阶段，保持静音
```

### 4. 倒计时最后10秒
- 应该听到 "哔~哔~哔~" 声
- BGM 应该停止

### 5. 杀手出现
- 全场静音
- 杀手移动时有脚步声

## 常见问题

### Q: 控制台显示什么？
打开浏览器开发者工具 (F12)，查看 Console 标签，搜索 `[音效]`

### Q: 完全没有日志输出？
检查：
1. `audio/game-bgm.mp3` 文件是否存在
2. `js/game-audio-manager.js` 是否被正确加载
3. 浏览器控制台是否有 JavaScript 错误

### Q: 日志显示"BGM播放被阻止"
这是正常的浏览器自动播放策略。解决方法：
1. 点击页面任意位置
2. 或者在浏览器设置中允许该网站自动播放音频

### Q: 点击后还是没声音？
检查：
1. 系统音量是否打开
2. 浏览器标签页是否静音（标签页上的小喇叭图标）
3. 音效按钮是否显示 🔇（静音状态）
4. 尝试点击音效按钮切换

## 浏览器自动播放策略

不同浏览器的自动播放策略：

| 浏览器 | 自动播放策略 |
|-------|------------|
| **Chrome** | 需要用户交互或网站已有交互历史 |
| **Firefox** | 需要用户交互 |
| **Safari** | 严格限制，几乎都需要用户交互 |
| **Edge** | 与 Chrome 相同 |

**建议：** 首次访问时，先点击一下页面任何位置，激活音频权限。

## 调试命令

在浏览器控制台中输入：

```javascript
// 检查音效管理器是否存在
window.gameAudioManager

// 手动播放 BGM
window.gameAudioManager.bgm.play()

// 检查当前音量
window.gameAudioManager.bgm.volume

// 检查是否静音
window.gameAudioManager.isMuted

// 手动触发游戏开始
window.gameAudioManager.onGameStart()
```

## 文件检查

确认以下文件存在：
- ✅ `audio/game-bgm.mp3` (你的背景音乐)
- ✅ `js/game-audio-manager.js` (音效管理器)
- ✅ `game-platform.html` 中引入了音效管理器

## 下一步

如果还是不行，请提供：
1. 浏览器控制台的完整日志（搜索 `[音效]`）
2. 浏览器类型和版本
3. 是否有 JavaScript 错误

---

**更新时间**: 2025-11-28  
**版本**: 1.2.1 (简化版)
